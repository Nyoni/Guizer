cmake_minimum_required(VERSION 3.18)

set(This GuizerTests)

set(Sources 
    GuizerTests.cpp
)

add_executable(${This} ${Sources})
target_link_libraries(${This} PUBLIC
    gtest_main
    Guizer
)

add_test(
    NAME ${This}
    COMMAND ${This}
)